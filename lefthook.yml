# 推送前对 web-antd 跑类型检查，拦截未声明依赖等 TS 模块解析错误
# turbo 有缓存：web-antd 源码未变更时命中缓存几乎瞬时完成；
# 有变更时约 10 秒。失败则推送被拦截。
pre-push:
  jobs:
    - name: typecheck web-antd
      run: pnpm exec turbo run typecheck --filter=@vben/web-antd
